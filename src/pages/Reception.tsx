import { Button, Card, Col, Row, Statistic, Typography } from 'antd';
import { ListChecks, QrCode, Users } from 'lucide-react';
import { useStore } from '../state/useStore';
import { appointmentRepository, queueRepository } from '../domain/repositories';
const { Title, Text } = Typography;
export default function Reception() { const tickets=useStore(queueRepository); const appointments=useStore(appointmentRepository); return <div style={{display:'flex',flexDirection:'column',gap:18}}><div><Text type="secondary">TIẾP ĐÓN</Text><Title level={3}>Trung tâm điều phối lễ tân</Title></div><Row gutter={[16,16]}><Col xs={24} md={8}><Card><Statistic title="Lịch hẹn sắp tới" value={appointments.filter(a=>a.status==='upcoming').length}/></Card></Col><Col xs={24} md={8}><Card><Statistic title="Đang chờ" value={tickets.filter(t=>t.status==='waiting').length}/></Card></Col><Col xs={24} md={8}><Card><Statistic title="Đang gọi/phục vụ" value={tickets.filter(t=>['called','in_service'].includes(t.status)).length}/></Card></Col></Row><Row gutter={[16,16]}><Col xs={24} md={12}><Card title="Check-in bệnh nhân"><Text>Quét và xác thực QR lịch hẹn tại quầy.</Text><Button type="primary" block icon={<QrCode size={16}/>} href="/app/reception/qr-check-in" style={{marginTop:16}}>Mở máy quét QR</Button></Card></Col><Col xs={24} md={12}><Card title="Điều phối hàng đợi"><Text>Gọi số, xác nhận bệnh nhân và bắt đầu phục vụ.</Text><Button block icon={<ListChecks size={16}/>} href="/app/reception/queue" style={{marginTop:16}}>Mở hàng đợi</Button></Card></Col></Row><Button icon={<Users size={16}/>} href="/app/appointments">Tra cứu lịch hẹn</Button></div>; }
